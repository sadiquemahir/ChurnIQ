# Minimal deps for unit tests (churn_utils only). Keeps CI fast and avoids
# optional heavy packages (xgboost, shap, streamlit) that can fail on some runners.
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0
pytest>=7.4.0
